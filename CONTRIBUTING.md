# 贡献指南

感谢你参与 **Programming Languages Mastery**。本项目是一本长期维护的开源教材，为了让全书读起来"像一个人写的"，所有贡献都需遵循以下规范。

> 本文件是全项目的写作与协作事实源。任何拿不准的格式问题，以此为准。

---

## 一、核心原则

所有内容都必须服务于四条原则（详见 [README](./README.md)）：

1. **概念优先** — 先讲"为什么需要"，再讲"怎么用"。
2. **横向对比** — 每个公共概念同时比较 JavaScript / Python / Java / C++ / C#，SQL 在相关章节加入。
3. **第一性原理** — 自底向上解释 `CPU → 内存 → Runtime → 语言`，触及真实机制而非停在 API。
4. **工程导向** — 每章落到真实项目：最佳实践、常见坑、性能、可运行示例。

---

## 二、语言与多语言规范 ⭐

这是本项目最重要的硬性约定，**所有文档一律遵守**。

### 1. 单文档单语言

**绝不在同一个文档里中英混排**——不写 `Vision｜愿景` 这类双语标题，不做逐行双语对照。一个文档要么全中文，要么全英文。

**唯一例外**（属于名称/标识符，不算"混排"）：
- 项目品牌名：`Programming Languages Mastery`
- 结构标签：`Part 1`、`v1.0`
- 技术标识符：`Runtime`、`GC`、`JVM`、`V8`、`CLR`、`RAII`、`SQL`、`ORM` 等编程术语，按惯例保留原文（它们也是代码里的写法）

### 2. 命名格式：默认中文 + `<名>.<locale>.md`

- **默认语言是简体中文**，中文文档**不带任何后缀**：`README.md`、`ROADMAP.md`、`07-变量.md`。
- **其他语言用完整 locale 后缀**：`<基名>.<locale>.md`。locale 采用**完整格式**（语言-地区），而不是简写：
  - ✅ `README.en-US.md`
  - ❌ `README.en.md`
- 示例：

  | 文档 | 简体中文（默认） | 英文（en-US） |
  |------|-----------------|---------------|
  | 项目说明 | `README.md` | `README.en-US.md` |
  | 学习路线 | `ROADMAP.md` | `ROADMAP.en-US.md` |
  | 章节 | `docs/…/08-variables.md` | `docs/…/08-variables.en-US.md` |

- 将来若增加其他语言，同样用完整 locale，例如 `zh-TW`（繁体）、`ja-JP`（日文）。

### 3. 语言切换索引

每个有多语言版本的文档，**顶部都要放语言切换索引**，并标出当前所在语言（当前语言加粗不加链接）：

```markdown
<!-- 中文版顶部 -->
**简体中文** ｜ [English](./README.en-US.md)

<!-- 英文版顶部 -->
[简体中文](./README.md) ｜ **English**
```

### 4. 事实源与同步节奏

- **简体中文版是事实源（source of truth）**，一切以中文版为准。
- 英文（及其他语言）版本在**每个版本里程碑同步**（即每次打 `v0.x` tag 之前统一对齐），**不要求逐条跟改**——避免维护成本失控。
- 提交只改了中文版时，可在 PR 里标注 `需同步 en-US`，留待里程碑统一处理。

---

## 三、目录与文件命名

- 章节文件按 `NN-english-slug.md` 命名（英文短横线 slug，与文件夹命名一致、URL 友好），`NN` 为两位数全书编号（`01`–`59`，全书连续），放入对应 `docs/PartX-*/` 目录。例如 `01-programming-history.md`、`08-variables.md`；英文版加后缀：`08-variables.en-US.md`。
- 示例代码放 `examples/<语言>/<概念>/`，六门语言目录名固定：`javascript` `python` `java` `cpp` `csharp` `sql`。
- 图片放 `images/`，图示源用 Mermaid 内联，不提交二进制绘图文件。

---

## 四、Markdown 排版规范

- 标题层级从 `#` 到 `###`，不跳级、不使用 `####` 以下。
- **中文加粗别紧贴标点**：`**` 不要紧贴中文标点（引号 `“”`、括号 `（）` 等）。原则——让 `**` 内侧贴汉字、外侧别贴标点，也就是把标点挪到加粗**外面**。✅ `“**短语**”`、`**中间表示**（IR）`；❌ `**“短语”**`、`**中间表示（IR）**`。原因是 CommonMark 的强调定界规则：`**` 夹在汉字与标点之间时既不能开始、也不能结束加粗，导致整段不渲染。**自查法**：提交前逐行用 CommonMark 渲染，若输出里仍残留字面 `**`，即该处加粗未生效。
- **链接规范**：
  - **站内互链**（章节之间、README/ROADMAP、语言切换）用**相对路径的 Markdown 链接**（如 `[ROADMAP](./ROADMAP.md)`），**同标签打开**——站内导航不宜每次新开窗口，且相对路径需交由文档站正确改写。
  - **站外链接**（参考资料、外部网站）统一在**新标签打开**：写成 `<a href="…" target="_blank" rel="noopener">标题</a>`。
  - 注意：GitHub 的 Markdown 渲染会忽略 `target` 属性（原样点击为同标签）；新窗口行为最终由部署的**文档站**（MkDocs/Docusaurus）统一保证。
- **代码块必须标注语言**：写 ` ```python ` 而不是裸 ` ``` `。
- 表格、`admonition`（Tips / Warning / Note）风格全书统一。
- 中文与英文/数字之间保留一个空格（如"使用 `let` 声明"）。
- 一句一义，避免"最强""完美""唯一正确"等绝对化表述；对比时说明取舍。

---

## 五、章节写作规范

- **Part 2 起**的概念章严格遵循 [docs/template.md](./docs/template.md) 的**完整 19 节结构**，一节都不能少；**Part 1** 等叙述/原理型章节使用同文件里的 **「Part 1 概念章模板（变体）」**（去掉逐语言代码节）。
- 每章至少包含：**1 张五语言语法对比表 + 1 张底层实现对比表 + 1 张工程实践对比表**。
- 每章至少 **5 个常见坑**，并解释"为什么错"。
- 练习分四级：基础 / 提高 / 挑战（以及可选的综合）。
- 章末统一给出：一句话总结 + 检查清单 + 下一章预告。
- **延伸阅读必须是可访问的真实链接**：每一条延伸阅读都要配一个**经过验证、当前可访问**的 URL（提交前实测，如返回 4xx/5xx 或已失效则不得使用）；**没有可访问链接的资源，就不要列入延伸阅读**；**严禁编造**不存在的书目或网页。链接本身遵循上文「链接规范」（站外链接新标签打开）。

---

## 六、代码与示例规范

- **贯穿全书的统一案例域**，不得随意更换主题：`Student`（学生）、`Library`（图书）、`Todo`、`Blog`、`Chat`。
  - 反例：Python 用 `Animal`、Java 用 `Person`、C++ 用 `Car`——这会破坏横向对比。
- 同一概念在六门语言里应实现**同一个例子**，字段、命名尽量一致。
- **所有示例必须真实运行通过后才能写入书中**；能配单元测试的尽量配。

### 运行示例

**一键验证（推荐）**：`examples/run-all.sh` 会自动跑遍所有示例并汇总结果，缺少某语言运行时会「跳过」而非「失败」。

```bash
cd examples
./run-all.sh              # 跑全部概念
./run-all.sh variables    # 只跑某个概念
./run-all.sh -v           # 同时显示每个示例的输出
```

全部通过时退出码为 `0`，有失败则为 `1`，可直接接入 CI。

**手动运行**，各语言命令如下：

| 语言 | 运行方式 |
|------|---------|
| JavaScript | `node variables.js` |
| Python | `python3 variables.py` |
| Java | `javac Variables.java && java Variables` |
| C++ | `g++ -std=c++17 -o out variables.cpp && ./out` |
| C# | `dotnet new console -o app`，将示例复制为 `app/Program.cs`，再 `dotnet run --project app` |
| SQL | `sqlite3 :memory: < variables.sql` |

---

## 七、术语表（节选）

中文正文中，下列术语**保留英文原文**，且译名/写法全书统一，不得混用（例如不要"垃圾回收 / GC / 回收器"混着写）：

| 中文 | 固定写法 |
|------|---------|
| 运行时 | Runtime |
| 垃圾回收 | GC |
| 栈 | Stack |
| 堆 | Heap |
| 指针 | Pointer |
| 引用 | Reference |
| 线程 | Thread |
| 事件循环 | Event Loop |

> 完整术语表随章节推进持续补充。

---

## 八、图示规范

- 所有图一律使用 **Mermaid**（流程图 `flowchart`、类图 `classDiagram`、时序图 `sequenceDiagram`、状态图 `stateDiagram-v2`）。
- 禁止提交 Visio / draw.io 导出的图片或截图。
- 图中文字与所在文档语言保持一致。

---

## 九、Git 提交与分支

- 提交信息用简洁的祈使句，建议前缀：`docs:` `example:` `fix:` `chore:`。
- 一个 PR 聚焦一件事（一章、一个概念、一处修订）。
- 涉及结构性调整（目录、编号、模板）需先在 Issue 讨论。

---

## 十、版本规范

本项目遵循**语义化版本（Semantic Versioning）**：

- `v0.x` — 逐 Part 推进的开发版本（当前处于此阶段）。
- `v1.0` — 第一版，八个 Part 全部完成。
- `v2.0+` — 扩展新语言（Go / Rust / Kotlin …）。

每次发布都在 [CHANGELOG.md](./CHANGELOG.md) 记录变更。

---

再次感谢你的贡献。是每一个人的用心，让这本书能陪伴更多人的职业生涯。
